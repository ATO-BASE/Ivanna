# Copy this file to `.env` and fill values before running the chat server
# Never commit your real OpenAI key to source control

# Your OpenAI server-side API key (keep secret)
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY

# URL where the chat proxy will run locally (used by the frontend)
# If you run the chat server locally with default settings, use:
VITE_OPENAI_API_URL=http://localhost:3001
